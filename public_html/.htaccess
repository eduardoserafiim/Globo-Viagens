Options -MultiViews
RewriteEngine On

# Mantém rotas amigáveis sem extensão, sem tentar redirecionar para caminhos absolutos do sistema.
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([A-Za-z0-9_-]+)$ $1.php [L,END]
